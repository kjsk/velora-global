{
  "name": "velora-global",
  "version": "1.0.0",
  "description": "Shipping and logistics web application with currency conversion",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && node index.js",
    "dev": "cd server && npm start",
    "build": "cd client && npm run build",
    "install-server": "cd server && npm install",
    "install-client": "cd client && npm install",
    "postinstall": "npm run install-server && npm run install-client"
  },
  "keywords": ["shipping", "logistics", "currency", "react", "nodejs"],
  "author": "Velora Global",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}